import 'package:flutter/widgets.dart';

const taskListPadding = 20.0;
const taskListItemSpacing = 12.0;

const startTaskListItemEdgeInsets = EdgeInsets.only(
  left: taskListPadding,
  right: taskListPadding,
);

const middleTaskListItemEdgeInsets = EdgeInsets.only(
  top: taskListItemSpacing,
  left: taskListPadding,
  right: taskListPadding,
);

const endTaskListItemEdgeInsets = EdgeInsets.only(
  top: taskListItemSpacing,  
  left: taskListPadding,
  right: taskListPadding,
);
